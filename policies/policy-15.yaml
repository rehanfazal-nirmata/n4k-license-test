apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: repo-policy-15
spec:
  rules:
    - name: check-label-15
      match:
        any:
          - resources:
              kinds:
                - Pod
      validate:
        message: "repo-managed test policy 15 (rename/restructure batch)"
        pattern:
          metadata:
            labels:
              test: "?*"
