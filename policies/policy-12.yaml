apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: repo-policy-12
spec:
  rules:
    - name: check-label-12
      match:
        any:
          - resources:
              kinds:
                - Pod
      validate:
        message: "repo-managed test policy 12 (rename/restructure batch)"
        pattern:
          metadata:
            labels:
              test: "?*"
